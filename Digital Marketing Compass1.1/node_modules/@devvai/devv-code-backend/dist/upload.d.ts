import { UploadFileResponse, ErrorResponse } from './types';
declare class FileUpload {
    uploadFile(file: File): Promise<UploadFileResponse>;
    isErrorResponse(response: UploadFileResponse): response is ErrorResponse;
}
export { FileUpload };
