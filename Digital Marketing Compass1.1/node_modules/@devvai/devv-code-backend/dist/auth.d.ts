import type { AuthResponse } from './types';
export declare class DevvAuth {
    sendOTP(email: string): Promise<void>;
    verifyOTP(email: string, verificationCode: string): Promise<AuthResponse>;
    logout(): Promise<void>;
}
